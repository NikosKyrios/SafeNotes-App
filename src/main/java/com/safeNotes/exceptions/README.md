
Custom exceptions used across the application.

- Purpose: represent domain-specific error conditions (auth, crypto, storage).
- What you'll find: `AuthException`, `EncryptionException`, `StorageException`, etc.
