
Repository layer: persistence abstraction and implementations.

- Purpose: CRUD access to storage (SQLite, files, or other data stores).
- What you'll find: interfaces like `UserRepository` and implementations like `UserRepositoryImpl`.
